node_modules
dist
.DS_Store
*.local

# artefactos de build de TypeScript
*.tsbuildinfo

# referencias del cliente, no forman parte del build
servicios y productos/

# originales entregados por el cliente (158 MB en PNG): del repo solo salen
# las versiones optimizadas que viven en public/img
imagenes1/
menus/
menu recursos/
video para el subheros/

# configuracion SMTP real: vive fuera de la web, nunca en el repositorio
linkdicom-smtp.php
nuevos requerimientos/

# panel de administracion: lo que se genera al usarlo nunca va al repositorio
# (en produccion vive en el servidor; en desarrollo, en linkdicom-dev/)
linkdicom-dev/
linkdicom-datos/
public/datos/*
!public/datos/.htaccess
public/media/*
!public/media/.htaccess
dos fotos/

# maquetas y originales del cliente para Radiologo Nacional (viven en su propio repositorio)
new proyec/
